[project]
name = "odoo-bringout-oca-queue-queue_job_web_notify"
version = "16.0.0"
description = "Queue Job Web Notify - 
        This module allows to display a notification to the related user of a
        failed job. It uses the web_notify notification feature."
authors = [
    { name = "Ernad Husremovic", email = "hernad@bring.out.ba" }
]
dependencies = [
    "odoo-bringout-oca-queue-queue_job>=16.0.0",
    "odoo-bringout-oca-queue-web_notify>=16.0.0",
    "requests>=2.25.1"
]
readme = "README.md"
requires-python = ">= 3.11"
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: GNU Lesser General Public License v3 (LGPLv3)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11", 
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business",
]

[project.urls]
homepage = "https://github.com/bringout/0"
repository = "https://github.com/bringout/0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["queue_job_web_notify"]

[tool.rye]
managed = true
dev-dependencies = [
    "pytest>=8.4.1",
]
