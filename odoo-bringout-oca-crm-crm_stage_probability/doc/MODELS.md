# Models

Detected core models and extensions in crm_stage_probability.

```mermaid
classDiagram
    class crm_lead
    class crm_stage
```

Notes
- Classes show model technical names; fields omitted for brevity.
- Items listed under _inherit are extensions of existing models.
